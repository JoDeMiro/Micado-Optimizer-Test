<VirtualHost *:80>
        ServerName 193.225.250.30
        ServerSignature Off
        ProxyPreserveHost On
        ProxyRequests Off
        # Your balanced backends

        <Proxy balancer://backend-cluster>
                # Ide kell beirni a Workerek belso halozati ip cimet
                BalancerMember http://192.168.0.xxx:8080
                BalancerMember http://192.168.0.xxx:8080
                BalancerMember http://192.168.0.xxx:8080
                BalancerMember http://192.168.0.xxx:8080
                BalancerMember http://192.168.0.xxx:8080
                BalancerMember http://192.168.0.xxx:8080
                BalancerMember http://192.168.0.xxx:8080

                ProxySet lbmethod=bybusyness
                ProxySet maxattempts=2
        </Proxy>
        # Balancer admin interface for localhost access
        <Location /balancer-manager>
                SetHandler balancer-manager
                Order allow,deny
                allow from all
        </Location>

        # mod_status admin interface for localhost access
        <Location /server-status>
                # SetHandler balancer-manager
                SetHandler server-status
                Order Deny,Allow
                Allow from all
        </Location>

        ProxyPass /balancer-manager !
        ProxyPass /server-status !
        # Proxying everything to your backends
        ProxyPass / balancer://backend-cluster/

</VirtualHost>

<Location /server-status>
    SetHandler server-status
    Order allow,deny
    allow from all
    AuthUserFile /etc/apache2/.htpasswd
    AuthType Basic
    AuthName "Make up a name here for who can access Apache status"
    Require user pisti
</Location>

# New
LoadModule status_module libexec/apache2/mod_status.so

# SetEnvIf Request_URI "^/actuator*" nelog
# CustomLog /var/log/apache2/nelog.log common env=nelog
# CustomLog /var/log/apache2/other_vhosts_access.log vhost_combined env=!nelog
# CustomLog /var/log/apache2/other_vhosts_access.log combined env=!nelog
# CustomLog /var/log/apache2/other_vhosts_access.log common env=!nelog


# New Fuck
# %D in log format
LogFormat "%v:%p %h %l %u %t \"%r\" %>s %O \"%{Referer}i\" \"%{User-Agent}i\" %D" vhost_combined
# LogFormat "%h %l %u %t \"%r\" %>s %O \"%{Referer}i\" \"%{User-Agent}i\" %D" combined
# LogFormat "%h %l %u %t \"%r\" %>s %O %D" common



